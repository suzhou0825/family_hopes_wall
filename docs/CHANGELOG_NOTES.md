# 沟通记录

## 2026-06-14

- 用户提出产品构想：孩子提交愿望，父母发布任务，孩子完成任务兑换愿望。
- 用户确认先不做苹果安装包，改为先提供 Web。
- 用户确认使用云端同步方向。
- 用户确认完成任务需要父母审核。
- 用户确认家庭成员由管理员手动添加。
- 用户要求将产品方案保存到目录下，后续沟通也持续记录。
- 用户要求本地开发端口从 3000 改为 6666。
- Next.js CLI 会拒绝 6666，原因是该端口被浏览器列为不安全端口；当前改为自定义开发服务器监听 6666。
- 用户确认不用 6666，改用 3333 作为本地开发端口。
- 用户反馈界面需要美化。
- 用户要求家庭设置除添加外支持编辑。
- 用户要求任务发布页、许愿板块除新增外支持编辑和删除。
- 用户第 4 点尚未补充完整。
- 用户要求打卡任务明确打卡频率，频率按周一到周日选择，并设置截止时间。
- 用户要求孩子可以每天在任务上打卡，任务看板展示打卡进度。
- 用户指出孩子发布愿望时缺少具体愿望描述和期望时间。
- 用户要求添加或编辑孩子时不需要父母身份，需要确认孩子性别，并可选择哥哥、弟弟、姐姐或妹妹。
- 用户询问家庭成员数据保存在哪里，并反馈修改后会回到初始错误数据；已改为使用浏览器 localStorage 保存本机原型数据。
- 用户指出发布任务时只有选择打卡任务才应出现打卡频率；已修正任务表单显示逻辑。
- 用户要求孩子可以在许愿墙主动申领任务，并且孩子登录后有自己的任务板块展示已申领任务和进展。
- 用户要求父母发布任务的奖励类型与孩子愿望类型保持一致，并且能输入具体奖励描述。
- 用户要求在项目目录下生成使用说明和开发约束文档，并要求每次更新时同步维护这两个文件。
- 用户要求先接 Supabase 数据库和登录；已接入 Supabase Auth，并新增 `app_state` 表保存完整应用数据。
- 用户提供 Supabase 项目地址和 publishable key；已写入 `.env.local`，项目 ref 为 `jqlgnsatgvwjadbzghfr`。
- 用户确认已在 Supabase SQL Editor 执行 `supabase/schema.sql`。
- 用户要求 Supabase 允许 GitHub 登录；已在前端新增 GitHub 登录按钮，并补充 Supabase/GitHub OAuth 后台配置说明。
- 用户反馈 GitHub 登录报错 `Unsupported provider: provider is not enabled`；已补充到使用说明的排查说明中。
- 用户确认 Supabase 已可用，并要求配置外网访问；已新增 Vercel 部署说明，并补充 Supabase 外网 URL 配置要求。
- 用户要求开发账户管理并记录到 Supabase 数据库，打开网页后必须账号密码登录；已新增登录门禁、账号管理表单和 `profiles` 表。
- 用户反馈页面卡在“正在检查登录状态”；已增加 Supabase 未配置和会话检查超时兜底，并补充部署排查说明。
- 用户要求本地调试、GitHub/Vercel 线上版本使用同一个 Supabase 数据库；已写入使用说明、开发约束、产品方案和部署说明。
- 用户要求注册支持邮箱或账号，注册时两次输入密码并校验一致，密码至少 8 位且包含字母和数字；已更新登录注册表单、校验逻辑和 profiles 表字段。
- 用户反馈普通账号注册报错 `family-hopes-wall.local is invalid`，并要求登录/注册密码框加可视按钮；已改用 `family-hopes-wall.app` 内部域名并新增显示/隐藏密码按钮。
- 用户反馈 `family-hopes-wall.app` 仍被 Supabase 判定为无效邮箱域名；已改为映射到当前 Supabase 项目真实域名 `jqlgnsatgvwjadbzghfr.supabase.co`。
- 用户明确要求每个人使用本应用自己的账号密码登录，不登录 Supabase Auth；已调整为 `app_accounts` 保存账号和密码哈希、`app_sessions` 保存会话、`app_data` 保存应用数据，前端通过 Supabase RPC 登录、注册、保存资料和保存数据。
- 用户反馈注册时报错 `function gen_salt(unknown) does not exist`；已将 `schema.sql` 中 pgcrypto 函数改为显式调用 `extensions` schema。
- 用户要求主动注册必须是父母账号，并在注册时选择爸爸或妈妈；父母进入软件后在家庭管理中添加孩子账号、初始密码、性别和哥哥/弟弟/姐姐/妹妹称呼；已改为家庭共享数据模型，孩子账号由父母创建并共享同一份家庭数据。
- 用户要求 Supabase 只保留当前系统需要的数据表；已在 `schema.sql` 中清理旧版 `profiles`、`app_state`、`app_data` 表，并记录当前仅保留四张核心表。
- 用户反馈注册妈妈账号后看不到爸爸和孩子；已明确主动注册会创建新家庭，并新增家庭管理中创建第二个父母账号的能力，确保爸爸、妈妈和孩子共享同一个家庭数据。
- 用户要求补充清空妈妈账号的逻辑；已新增删除家庭成员账号 RPC，家庭管理删除成员时会同步删除 Supabase 账号、会话和家庭数据中的成员记录。
- 用户反馈创建妈妈账号时报错“孩子性别只能选择男孩或女孩”；已修复父母账号创建时不再校验孩子性别和称呼，前端也会传入安全默认值。
- 用户要求登录后选择具体家庭角色；已将右上角角色切换改为展示爸爸、妈妈、哥哥、弟弟、姐姐或妹妹，而不是只显示父母/孩子。
